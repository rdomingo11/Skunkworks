@page "/fetchdata"

<PageTitle>SpaceX Launches</PageTitle>

<h1>SpaceX Launches</h1>

<p>This is the launch data pulled back via REST API</p>

@if (launches == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Is Tentative</th>
                <th>Launch Date Local</th>
                <th>Mission Name</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var launch in launches)
            {
                <tr>
                    <td>@launch.Id </td>
                    <td>@launch.IsTentative </td>
                    <td>@launch.LaunchDateLocal</td>
                    <td>@launch.MissionName </td>
                </tr>
            }
        </tbody>
    </table>
}